<MobileToolchainScreen>:
    BoxLayout:
        orientation: 'vertical'
        SlideTitleLabel:
            text: 'Mobile Toolchain'
        Label:
            font_size: app.font_size_large
            text: 'Android Toolchain'
        Label:
            text: 'Kivy -> python-for-android -> Buildozer -> [Hanga]'
        Label:
            font_size: app.font_size_large
            text: 'iOS Toolchain'
        Label:
            text: 'Kivy -> python-ios -> (Buildozer) -> (Hanga)'
